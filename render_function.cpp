#include <winsock2.h>
#include <windows.h>

// Function to draw pixels on the window
void DrawPixels(HDC hdc, int width, int height) {
    // for (int x = 0; x < width / 5; ++x) {
    //     for (int y = 0; y < height; ++y) {
    //         // Create a simple red gradient
    //         COLORREF color = RGB(255, x * 255 / width, y * 255 / height);
    //         SetPixel(hdc, x*5 + y, y, color);
    //     }
    // }

    // Rectangle(hdc, 50, 50, 250, 150);
}